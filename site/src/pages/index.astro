---
import BaseLayout from "../layouts/BaseLayout.astro";
import { INSTRUMENTS } from "../lib/instruments.ts";
---

<BaseLayout
  title="Tête de Moule — atelier des doigtés"
  description="Dépose une partition MuseScore, on fait les doigtés, tu imprimes ta partie."
>
  <section class="hero">
    <div class="shell">
      <h1>Atelier des doigtés</h1>
      <p class="hero__lead">
        Dépose une partition MuseScore. On fait les doigtés, tu imprimes ta
        partie.
      </p>
    </div>
  </section>

  <section class="section" id="atelier">
    <div class="shell">
      <div class="workbench">
        <div class="panel stack" style="--gap: 0">
          <h2 class="ui" style="font-size:1.1rem">Réglages</h2>

          <label class="field">
            <span class="field__label">Instrument</span>
            <select id="instrument">
              {INSTRUMENTS.map((instrument) => (
                <option value={instrument.id}>{instrument.label}</option>
              ))}
            </select>
            <span class="field__hint" id="instrument-hint">Retenu pour la prochaine fois.</span>
          </label>

          <label class="field">
            <span class="field__label">Lecture</span>
            <select id="reading"></select>
          </label>

          <label class="field" id="valves-field">
            <span class="field__label">Pistons</span>
            <select id="valves"></select>
          </label>

          <div class="field">
            <span class="field__label">Afficher</span>
            <div class="toggles">
              <label class="toggle"><input type="checkbox" id="show-fingerings" checked /> Doigtés</label>
              <label class="toggle"><input type="checkbox" id="show-note-names" checked /> Noms des notes</label>
              <label class="toggle"><input type="checkbox" id="show-measure-numbers" checked /> N° de mesure</label>
            </div>
          </div>
        </div>

        <div class="stack" style="--gap: 1rem">
          <div id="dropzone" class="dropzone" tabindex="0" role="button"
               aria-label="Choisir une ou plusieurs partitions MuseScore">
            <p class="dropzone__title">Dépose tes partitions ici</p>
            <p class="muted small" style="margin:0">.mscz — une ou plusieurs, ou clique pour choisir</p>
            <input type="file" id="file" accept=".mscz,.mscx" multiple class="hidden" />
          </div>

          <div id="messages" class="stack" style="--gap: 0.6rem"></div>

          <div id="sheets" class="sheets hidden"></div>

          <div id="actions" class="button-row hidden">
            <button class="button button--quiet" id="reset" type="button">Tout effacer</button>
            <span class="small muted" id="status"></span>
          </div>

          <div id="preview" class="preview hidden">
            <div class="preview__bar">
              <button class="button button--quiet preview__arrow" id="page-prev" type="button"
                      aria-label="Page précédente">‹</button>
              <span class="small muted" id="page-counter"></span>
              <button class="button button--quiet preview__arrow" id="page-next" type="button"
                      aria-label="Page suivante">›</button>
            </div>
            <div class="preview__pages" id="pages"></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="section" id="a-venir">
    <div class="shell">
      <div class="section__head">
        <h2>La suite</h2>
        <p class="muted">Au rythme des répétitions.</p>
      </div>
      <div class="grid-cards">
        <article class="card">
          <h3>Tableau des doigtés</h3>
          <p class="muted small" style="margin:0">
            Toutes les combinaisons, et les notes que donne chacune.
          </p>
        </article>
        <article class="card card--soon">
          <h3>Le répertoire</h3>
          <p class="small" style="margin:0">Les morceaux de la fanfare, avec la partie de chacun.</p>
        </article>
        <article class="card card--soon">
          <h3>Écouter sa partie</h3>
          <p class="small" style="margin:0">Jouer sa ligne seule, ou par-dessus le reste.</p>
        </article>
      </div>
    </div>
  </section>

  <script>
    import "../scripts/app.ts";
  </script>
</BaseLayout>
